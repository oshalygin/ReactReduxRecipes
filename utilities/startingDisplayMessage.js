import colors from "colors";

/* eslint-disable no-console */

const displayMessage = "Starting application in development mode...";
const loggedMessage = displayMessage.green;

console.log(loggedMessage);
